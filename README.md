# Java Game of Life Viewer

Simple split-screen application:
- **Left**: Java Game of Life terminal output (live)
- **Right**: Trefoil Knot GIF animation

## Running

```bash
cd /data/projects/java-life-viewer
docker-compose up --build
```

Open in browser: **http://localhost:8000**

## Stopping

```bash
docker-compose down
```
